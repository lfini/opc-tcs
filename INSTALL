Istruzioni di installazione

L'installazione consiste semplicemente nell'estrarre i files dall'archivio in
una directory dedicata.

le procedure si possono lanciare come nell'esempio:

       python  <percorso...>/dtracker.py

NOTA: cambiando nome in dtracker.pyw si può lanciare la procedura in modo
      grafico (senza la finestra del prompt).
